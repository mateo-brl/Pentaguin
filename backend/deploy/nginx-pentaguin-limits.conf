# /etc/nginx/conf.d/pentaguin-limits.conf
# Zones de rate-limit de l'API Pentaguin (contexte http, requis par nginx).
limit_req_zone $binary_remote_addr zone=pentaguin_req:1m rate=10r/s;
limit_conn_zone $binary_remote_addr zone=pentaguin_conn:1m;
