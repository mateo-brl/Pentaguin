// Déclaration versionnée pour les imports CSS (web uniquement) : expo-env.d.ts,
// qui la fournit normalement, est généré par `expo start` et gitignoré — la CI
// doit pouvoir typechecker sans lui.
declare module '*.css';
