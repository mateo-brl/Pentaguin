/** Barème XP — alimente daily_activity, donc le streak et le futur classement (v1.1). */
export const XP = {
  correctAnswer: 10,
  lessonCompleted: 20,
  /** Bonus de fin de mission (en plus de l'XP des exercices qui la composent). */
  missionCompleted: 30,
} as const;
